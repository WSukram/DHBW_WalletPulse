services:
  backend:
    environment:
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      CORS_ALLOWED_ORIGINS: "https://walletpulse.de,https://www.walletpulse.de,https://wsukram.github.io"

  frontend:
    ports:
      - "3000:80"
