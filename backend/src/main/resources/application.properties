# This file is excluded from the Docker image (see backend/.dockerignore).
# When running via Docker Compose all configuration comes from environment variables in .env.
# This file is only loaded by the test suite — keep it minimal.

# Required: no default in code, not set by test properties
cors.allowed-origins=http://localhost:5173,http://localhost:3000

# GraphiQL is disabled by default; must be enabled here for GraphQlSmokeTest
spring.graphql.graphiql.enabled=true
